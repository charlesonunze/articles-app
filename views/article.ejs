<!DOCTYPE html>
<html>
<% include ./partials/head %>

<body style="font-family: Varela Round">

  <% include ./partials/nav %>

  <br>
  
  <div class="uk-container">
    <article class="uk-article">
        <h1><%= article.title %></h1>

        <p class="uk-article-meta">
          Written by 
          <a href="#"><%= article.author %></a>
        </p>

        <p class="uk-text-lead">
          <%= article.body %>
        </p>
    </article>
  </div>

  <% if (user) {%>
    <% if (user.username === article.author) {%>
        <div style="text-align: center">
        <a class="uk-button uk-button-danger" href="#modal-center" uk-toggle>delete article</a>
        <a href="/article/edit/<%= article._id %>" class="uk-button uk-button-primary">edit article</a>

        <div id="modal-center" class="uk-flex-top" uk-modal>
          <div class="uk-modal-dialog uk-modal-body uk-margin-auto-vertical">
              <button class="uk-modal-close-default" type="button" uk-close></button>
              <p>If you delete this article, it cannot be recovered.</p>

              <form class="" action="/article/<%= article._id %>" method="post">
                <input type="submit" name="" value="delete" class="uk-button uk-button-danger">
                <a class="uk-button uk-button-primary uk-modal-close">cancel</a>
              </form>
            </div>
          </div>
        </div>
      <% } %>
    <% } %>

  <% include ./partials/scripts %>
</body>
</html>